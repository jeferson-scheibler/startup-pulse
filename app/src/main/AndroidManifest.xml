<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StartupPulse"
        tools:targetApi="31">

        <!-- Splash = LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.StartupPulse.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demais telas -->
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".SignUpActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".CanvasIdeiaActivity"
            android:label="@string/adicionar_nova_ideia" />

        <activity
            android:name=".CanvasMentorActivity"
            android:label="@string/mentores" />

        <activity
            android:name=".AvaliacaoActivity"
            android:label="@string/avaliacao_titulo" />
        <activity
            android:name=".InvestorDetailActivity"
            android:label="@string/titulo_detalhe_investidor"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MentorDetailActivity"
            android:exported="false"
            android:label="@string/titulo_detalhe_mentor"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>
