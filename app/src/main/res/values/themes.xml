<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.StartupPulse" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorSurface">@color/background_color</item>
        <item name="android:colorBackground">@color/background_color</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="ShapeAppearance.App.Button.LessRounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item> <!-- << Altere este valor como quiser -->
    </style>

    <!-- 2. Cria um estilo para o botão preenchido (Salvar) usando o novo formato -->
    <style name="App.Button.LessRounded" parent="Widget.Material3.Button">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.App.Button.LessRounded</item>
    </style>

    <!-- 3. Cria um estilo para o botão com contorno (Cancelar) usando o novo formato -->
    <style name="App.Button.Outlined.LessRounded" parent="Widget.Material3.Button.OutlinedButton">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.App.Button.LessRounded</item>
    </style>
</resources>