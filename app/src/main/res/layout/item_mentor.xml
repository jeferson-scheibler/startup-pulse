<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="10dp"
    android:layout_marginBottom="2dp"
    android:clipToPadding="false">

    <!-- Card interno (superfície) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardMentor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="20dp"
        android:layout_marginHorizontal="12dp"
        android:layout_marginVertical="4dp"
        app:cardUseCompatPadding="false"
        app:cardElevation="2dp"
        app:strokeWidth="0dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_card_mentor"
            android:minHeight="32dp"
            android:paddingStart="10dp"
            android:paddingTop="12dp"
            android:paddingEnd="10dp"
            android:paddingBottom="14dp">

            <!-- Avatar -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/imageViewMentor"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:contentDescription="@string/mentor_avatar_content_desc"
                android:scaleType="centerCrop"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:src="@drawable/ic_person" />

            <!-- Nome + selo -->
            <LinearLayout
                android:id="@+id/rowNome"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                app:layout_constraintStart_toEndOf="@id/imageViewMentor"
                app:layout_constraintTop_toTopOf="@id/imageViewMentor"
                app:layout_constraintEnd_toStartOf="@id/imageViewArrow">

                <TextView
                    android:id="@+id/textViewNome"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textAppearance="?attr/textAppearanceTitleSmall"
                    android:textStyle="bold"
                    android:textColor="@color/colorOnSurface"
                    tools:text="DANTON YURI RUTZ" />

                <ImageView
                    android:id="@+id/iconVerificado"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_marginStart="6dp"
                    android:contentDescription="@string/mentor_verificado_content_desc"
                    android:src="@drawable/ic_verified"
                    android:visibility="gone"
                    app:tint="@color/green_success"
                    tools:visibility="visible" />
            </LinearLayout>

            <!-- Chips de áreas (compactos) -->
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupAreas"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                app:layout_constraintStart_toStartOf="@id/rowNome"
                app:layout_constraintEnd_toStartOf="@id/imageViewArrow"
                app:layout_constraintTop_toBottomOf="@id/rowNome"
                app:singleLine="true"
                app:singleSelection="false"
                tools:ignore="SpeakableTextPresent"/>

            <!-- Chevron -->
            <ImageView
                android:id="@+id/imageViewArrow"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="@string/ver_detalhes_mentor_content_desc"
                android:src="@drawable/ic_chevron_right"
                app:tint="@color/colorSecondary"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</FrameLayout>
