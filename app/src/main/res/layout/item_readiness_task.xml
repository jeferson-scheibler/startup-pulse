<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="12dp">

    <ImageView
        android:id="@+id/image_view_status"
        android:layout_width="24dp"
        android:layout_height="24dp"
        tools:src="@drawable/ic_check"
        tools:tint="@color/colorSuccess" />

    <TextView
        android:id="@+id/text_view_task_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_weight="1"
        android:textAppearance="@style/Text.BodyMedium"
        tools:text="Preencha todos os blocos do seu Canvas da Ideia." />

</LinearLayout>